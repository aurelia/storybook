/** @deprecated Import Storybook types from `storybook/internal/types` instead. */
export type {
  ArgsStoryFn,
  Renderer,
  RenderContext,
  StoryContext,
  WebRenderer,
} from 'storybook/internal/types';
